"""Quantitative evaluation utilities for Seiba sensitive data scanner.

This package is intentionally lightweight (stdlib-only) so it can run anywhere the
SDK runs. It is separate from `tests/` because it produces repeatable reports and
baselines, not just pass/fail assertions.
"""

from __future__ import annotations

__all__ = []

